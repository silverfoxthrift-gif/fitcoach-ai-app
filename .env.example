# The n8n webhook URL this app POSTs to.
# Local dev: use the workflow's TEST url (n8n must be in "Listen for test event" / execute mode).
# Production (Vercel): use the workflow's PRODUCTION url (workflow must be ACTIVATED).
# Kept server-side only — never exposed to the browser.
N8N_WEBHOOK_URL=
